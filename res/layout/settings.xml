<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent">

    <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textSize="18dp"
            android:text="Emergency SMS recipients (split by newline):"
            />

    <EditText
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            android:layout_weight="0.85"
            android:gravity="top|left"
            android:background="#333"
            android:id="@+id/emergency_sms_recipients"
            />

    <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textSize="18dp"
            android:text="Emergency call recipient:"
            />

    <EditText
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            android:layout_weight="0.15"
            android:background="#333"
            android:id="@+id/emergency_call_recipients"
            />

    <Space android:layout_width="fill_parent" android:layout_height="5dp"/>

    <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="Save"
            android:background="#ff4f59ba"
            android:textAllCaps="true"
            android:id="@+id/save_settings"
            />
</LinearLayout>